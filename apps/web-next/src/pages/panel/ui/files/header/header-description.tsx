import { BedrockText } from "@/shared/ui/bedrock-text";

export const HeaderDescription = () => (
  <BedrockText
    type="p"
    color="white"
    text={
      "Explore detailed download counts for each individual file. See exactly how many times each asset."
    }
  />
);
