export enum Routes {
    HOME = "/",
    DOWNLOADS = "/downloads",
    PANEL = "/panel",
    PANEL_ANALYTICS = "/panel/analytics",
    INFORMATION = "/information",
    FETCH = "/fetch",
    PREVIEW = "/preview"
}